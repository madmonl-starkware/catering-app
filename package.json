{
  "name": "freech",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev:client": "pnpm --filter @freech/client dev",
    "build:client": "pnpm --filter @freech/client build",
    "preview:client": "pnpm --filter @freech/client preview",
    "deploy:client": "pnpm --filter @freech/client run deploy"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "packages/contracts",
    "packages/starknet-contract-connect",
    "apps/client"
  ]
}